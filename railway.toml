[build]
builder = "NIXPACKS"

[deploy]
# Healthcheck disabled temporarily - EasyAppointments needs installation first
# healthcheckPath = "/"
# healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
